[pytest]
testpaths = tests
addopts = -ra
markers =
    validation: compared against an independent truth (analytic formula, NIST tabulation, hand calculation)
    characterization: compared against frozen reference values from a recorded run; detects change, asserts no independent truth
    library_value: pinned to the shipped nuclear-data libraries; expected to shift on a data-library rebuild (re-record deliberately)
